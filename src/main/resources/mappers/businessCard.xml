<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.infosiatec.mapper.BusinessCardMapper">

	<insert id="insertBusinessCard" parameterType="string">
		INSERT INTO businesscard (id, fileName, isDelete)
		VALUES ( #{id}, #{fileName}, FALSE )
	</insert>
	
	<select id="selectFileName" parameterType="com.infosiatec.domain.BusinessCardVO" resultType="string">
		SELECT fileName
		FROM businesscard
		WHERE id = #{id} AND idx = #{idx} AND isDelete='FALSE'
	</select>
	
	<select id="selectBusinessCardList" parameterType="string" resultType="com.infosiatec.domain.BusinessCardVO">
		SELECT *
		FROM businesscard
		WHERE isDelete='FALSE'
		ORDER BY idx
	</select>
	
	<select id="selectMaxIdx" resultType="Integer">
		SELECT MAX(idx)
		FROM businesscard
	</select>

	<update id="deleteBusinessCard">
		UPDATE businesscard set isDelete = TRUE
		where idx in 
		<foreach collection="idx" item="idx" open="(" close=")" separator=",">
			#{idx}
		</foreach>
	</update>
	
	<select id="searchBusinessCard" parameterType="string" resultType="com.infosiatec.domain.BusinessCardVO">
		SELECT *
		FROM businesscard
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="searchType=='title' and keyword != null and keyword != '' ">
				AND id like CONCAT('%', #{keyword}, '%')
			</if>

			<if test="searchType=='idx' and keyword != null and keyword != '' ">
				AND idx like CONCAT('%', #{keyword}, '%')
			</if>
	</trim>
		AND isDelete = FALSE
	</select>

</mapper>